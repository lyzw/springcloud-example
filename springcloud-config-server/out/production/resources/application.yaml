# 管理端口的开放配置
management:
  endpoints:
    web:
      exposure:
        include: env,encrypt
  endpoint:
    env:
      enabled: true
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:9010/eureka/
